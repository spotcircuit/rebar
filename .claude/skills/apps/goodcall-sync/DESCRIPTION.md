# goodcall-sync app skills

Skills specific to GoodCall ↔ HubSpot sync: API surface notes (reverse-engineered via dogfood), entity mapping, sync-conflict resolution. Load when working on the GoodCall integration.

## Status

Reserved namespace. The reverse-engineered GoodCall API surface lives in `clients/{client}/research/` from prior `/dogfood` runs. Per Hermes incorporation P1, the next pass uses the browser harness for re-probe.
