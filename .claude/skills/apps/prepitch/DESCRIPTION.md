# prepitch app skills

Skills specific to the Prepitch app: client-deliverable QA flows, pitch-doc structure, pre-meeting prep checklists. Load when working in `apps/prepitch/`.

## Status

Reserved namespace. No skills yet — current behavior lives in `apps/prepitch/expertise.yaml` and `apps/prepitch/specs/`.
