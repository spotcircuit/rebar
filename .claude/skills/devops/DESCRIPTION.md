# DevOps skills

Paperclip orchestration, worker patterns, webhook subscriptions, and runtime plumbing. Load when an agent is configuring routines, dispatching work, or wiring an external trigger.

## Skills here

_(Empty — staging Hermes ports.)_

Planned (per Hermes incorporation, P3/P4):
- `paperclip-orchestrator/` — KANBAN_GUIDANCE preamble + decompose-don't-execute pattern.
- `paperclip-worker/` — per-issue worker contract.
- `paperclip-webhook/` — webhook-subscription patterns.

## When to load this category

- Creating or modifying a Paperclip routine
- Wiring a webhook trigger or external dispatch
- Designing how a parent agent decomposes work for a worker agent

## Status

Reserved namespace. The `system/paperclip.yaml` schedules are the current state; this folder lands the Hermes patterns when ported.
